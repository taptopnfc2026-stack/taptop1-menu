name = "taptop-menu-ai"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Rate limiting per IP (100 requests per minute)
[[unsafe.bindings]]
name = "RATE_LIMITER"

[vars]
ALLOWED_ORIGINS = "https://taptopnfc2026-stack.github.io,http://localhost:5173,http://localhost:5174"
